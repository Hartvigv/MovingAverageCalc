import React from "react";

const Header = () => {
  const pageHeader = "Simple Moving Average-Calculator";
  return (
    <div className="Header">
      <h1 className="Header-h1">{pageHeader}</h1>
    </div>
  );
};

export default Header;
